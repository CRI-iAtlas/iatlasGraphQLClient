query SingleCellFeature(
      $paging: PagingInput
      $distinct:Boolean
      $feature: [String!]
      $cohort: [String!]
      $cell: [String!]
  ) {
      cells(
          paging: $paging
          distinct: $distinct
          feature: $feature
          cohort: $cohort
          cell: $cell
      ) {
        items {
            name
            type
            features {
                name
                display
                value
          }
        }
        paging {
            type
            pages
            total
            startCursor
            endCursor
            hasPreviousPage
            hasNextPage
            page
            limit
        }
        error
    }
}
